# ── Dev/test-only dependencies ────────────────────────────────────────────────
# Kept out of the prod requirements.txt so the production image (and the
# Render Python buildpack) doesn't carry pytest et al. CI installs both
# files: `pip install -r requirements.txt -r requirements-dev.txt`.

-r requirements.txt

pytest>=8.0,<9
pytest-asyncio>=0.23,<1
httpx>=0.27,<1          # FastAPI TestClient transitive
fakeredis>=2.21,<3      # in-process Redis for cache tests
